{
  "name": "coscribble-server",
  "version": "0.1.0",
  "private": true,
  "description": "Realtime sync server for CoScribble. There is one shared room per normalized page URL.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
